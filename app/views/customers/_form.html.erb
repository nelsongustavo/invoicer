<div class="row">
  <%= form_for(@customer) do |f| %>
    <% if @customer.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@customer.errors.count, "error") %> prohibited this customer from being saved:</h2>

        <ul>
        <% @customer.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    
    <div class="row">
      <div class="input-field col s6">
        <%= f.label :enrolment, "Matricula" %><br>
        <%= f.text_field :enrolment %>
      </div>
      <div class="input-field col s6">
        <%= f.label :name, "Nome" %><br>
        <%= f.text_field :name %>
      </div>
    </div>
    
    <div class="row">
      <div class="input-field col s6">
        <%= f.label :cpf, "CPF" %><br>
        <%= f.text_field :cpf %>
      </div>
      <div class="input-field col s6">
        <%= f.label :email, "Email" %><br>
        <%= f.text_field :email %>
      </div>
    </div>
    
    <div class="row">
      <div class="input-field col s12">
        <%= f.label :address, "Endereço" %><br>
        <%= f.text_area :address %>
      </div>
    </div>

    <div class="row">
      <div class="col s12">
        <button class="btn waves-effect waves-light modal-close" type="submit" name="action">Salvar
          <i class="material-icons right">send</i>
        </button>
      </div>
    </div>
  <% end %>
</div>